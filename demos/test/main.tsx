import React from 'react';
import ReactDOM from 'react-dom/client';

const jsx = <div>11 tiny-react 22</div>;

const root = document.querySelector('#root');

ReactDOM.createRoot(root).render(jsx);

console.log(React);
console.log(jsx);
console.log(ReactDOM);
